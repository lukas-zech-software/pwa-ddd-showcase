export enum ImageTab {
  UPLOAD,
  STOCK,
  HISTORY,
}

export enum WizardStep {
  KIND,
  TYPE,
  DESCRIPTION,
  DETAILS,
  VALUE,
  DATE,
  IMAGE,
  SUMMARY,
}

export enum WizardMode {
  CREATE,
  EDIT,
  TEMPLATE,
}
