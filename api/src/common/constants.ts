/* istanbul ignore next: useless to test constants */

/**
 * The reflect key for the router meta data used by the {@link WebRouteDecorator}
 * @type {string}
 */
export const ROUTE_META_DATA_KEY = 'ROUTE_META_DATA_KEY';

export const ACCESS_TOKEN_FIELD = 'Authorization';

export const CACHE_CLEAR_SECRET = 'some-secret-password';

export const noop = (): void => {
};
